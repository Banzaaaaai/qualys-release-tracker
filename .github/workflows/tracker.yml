name: Qualys Release Tracker

on:
  schedule:
    # Morning run — 08:10 Amsterdam (CEST, summer)
    # DST adjuster patches this line automatically each Oct/Mar
    - cron: "10 6 * * *"
    # Afternoon run — 16:00 Amsterdam (CEST, summer)
    # DST adjuster patches this line automatically each Oct/Mar
    - cron: "0 14 * * *"
    # Monthly digest — first day of each month at 08:00 UTC
    - cron: "0 8 1 * *"
  workflow_dispatch:
    inputs:
      force_notify:
        description: "Send notification even if no new releases (for testing)"
        required: false
        default: "false"
      monthly_digest:
        description: "Send monthly digest email now (true/false)"
        required: false
        default: "false"
      digest_month:
        description: "Digest target month, e.g. 2026-06 (optional; blank = current month)"
        required: false
        default: ""

permissions:
  contents: write   # needed to commit snapshot.json, run_log.json, badge.json

jobs:
  track:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      # ── 1. Checkout ────────────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 1

      # ── 2. Python setup ────────────────────────────────────────────────────
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      # ── 3. Detect if this is a monthly-digest run ──────────────────────────
      - name: Set MONTHLY_DIGEST flag
        id: flags
        run: |
          # True only when triggered by the monthly-digest cron specifically,
          # OR manually. Matching on the exact cron (not day-of-month) avoids
          # misfiring on the two daily runs that also land on the 1st.
          SCHEDULE="${{ github.event.schedule }}"
          MANUAL="${{ github.event.inputs.monthly_digest }}"
          if [[ "$SCHEDULE" == "0 8 1 * *" ]] || [[ "$MANUAL" == "true" ]]; then
            echo "monthly_digest=true" >> "$GITHUB_OUTPUT"
          else
            echo "monthly_digest=false" >> "$GITHUB_OUTPUT"
          fi

      # ── 4. Run tracker ─────────────────────────────────────────────────────
      - name: Run release tracker
        env:
          SMTP_HOST:      ${{ secrets.SMTP_HOST }}
          SMTP_PORT:      ${{ secrets.SMTP_PORT }}
          SMTP_USER:      ${{ secrets.SMTP_USER }}
          SMTP_PASSWORD:  ${{ secrets.SMTP_PASSWORD }}
          EMAIL_TO:       ${{ secrets.EMAIL_TO }}
          FORCE_NOTIFY:   ${{ github.event.inputs.force_notify }}
          MONTHLY_DIGEST: ${{ steps.flags.outputs.monthly_digest }}
          DIGEST_MONTH:   ${{ github.event.inputs.digest_month }}
          LOG_LEVEL:      INFO
        run: python scraper.py

      # ── 5. Commit updated files ────────────────────────────────────────────
      - name: Commit updated files
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add snapshot.json run_log.json badge.json
          # Also add archive file if it was created
          git add snapshot_archive.json 2>/dev/null || true
          git diff --cached --quiet || git commit -m \
            "chore: tracker run $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push
